using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class shoot : MonoBehaviour {
    public GameObject p;
    public Camera camera1;
	// Use this for initialization
	void Start () {
       //this.gameObject.transform.parent= GameObject.FindWithTag("target").transform;
    }
	
	// Update is called once per frame
	void Update () {

       // gameObject.transform.position = new Vector3(camera1.pixelHeight/2,camera1.pixelHeight/2);
	}
}
